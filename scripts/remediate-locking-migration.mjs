import fs from 'node:fs'

function replaceOnce(source, before, after, label) {
  const count = source.split(before).length - 1
  if (count !== 1) throw new Error(`Expected exactly one ${label}, found ${count}`)
  return source.replace(before, after)
}

const path='src/data/localDatabase.ts'
let source=fs.readFileSync(path,'utf8')
source=replaceOnce(source,
  "import { legacyRecordId, singletonRecordId, validateRevisionGraph, type Revision } from '../security/revisions'",
  "import { createMergeRevision, legacyRecordId, singletonRecordId, validateRevisionGraph, type Revision } from '../security/revisions'",
  'revision import')
source=replaceOnce(source,
  "async function recordIdentity(context:EpochContext,store:LocalStoreName,id:string):Promise<string>{const profile=profileFor(store,id);return store===LOCAL_STORES.settings?singletonRecordId(context.diaryId,profile.recordType as 'pain_type_settings'|'activity_type_settings'):legacyRecordId(context.diaryId,profile.recordType,id)}",
  "async function recordIdentity(context:EpochContext,store:LocalStoreName,id:string):Promise<string>{const profile=profileFor(store,id);return store===LOCAL_STORES.settings?singletonRecordId(context.diaryId,profile.recordType as 'pain_type_settings'|'activity_type_settings'):legacyRecordId(context.diaryId,profile.recordType,id)}\nfunction compareProtocolIds(left:string,right:string):number{const a=fromBase64Url(left),b=fromBase64Url(right),length=Math.min(a.byteLength,b.byteLength);for(let index=0;index<length;index++){const delta=a[index]!-b[index]!;if(delta!==0)return delta}return a.byteLength-b.byteLength}\nfunction sortedRevisionIds(ids:Iterable<string>):string[]{return [...ids].sort(compareProtocolIds)}",
  'record identity helper')
source=replaceOnce(source,
  "async function persistRevision(db:IDBDatabase,store:LocalStoreName,value:Record<string,unknown>,fixedRevisionId?:string):Promise<void>{",
  "async function persistRevision(db:IDBDatabase,store:LocalStoreName,value:Record<string,unknown>,fixedRevisionId?:string,writeMode:'normal'|'merge'|'test-branch'='normal',explicitParents:readonly string[]=[]):Promise<void>{",
  'persist signature')
source=replaceOnce(source,
  "const graph=validateRevisionGraph(revisions),heads=[...(graph.headsByRecord.get(recordId)??[])],revisionId=fixedRevisionId??base64Url(randomBytes(32));if(heads.length>1)throw new Error('Unresolved record conflict; explicit merge is required.')\n  if(revisions.some(existing=>existing.revision_id===revisionId))return\n  const revision:Revision={record_type:profile.recordType,record_schema:profile.recordSchema,record_id:recordId,revision_id:revisionId,parent_revision_ids:heads,record_status:value.status==='deleted'?'deleted':'active',record_data:revisionData(value),migration_origin:null,protocol_created_at:new Date().toISOString()}\n  validateRevision(revision)\n  if(revision.record_status==='deleted'){if(revision.record_data!==null)throw new Error('Deleted records must contain null data.')}else validateDomainData(DOMAIN_SCHEMAS[revision.record_schema],revision.record_data)",
  "const graph=validateRevisionGraph(revisions),heads=sortedRevisionIds(graph.headsByRecord.get(recordId)??[]),revisionId=fixedRevisionId??base64Url(randomBytes(32));if(writeMode==='normal'&&heads.length>1)throw new UnresolvedRecordConflictError(recordId,heads);if(writeMode==='merge'&&heads.length<2)throw new Error('Record does not have multiple heads to merge.');if(writeMode==='merge'&&value.status==='deleted')throw new Error('Explicit merge requires an active merged value.');if(writeMode==='test-branch'&&import.meta.env.MODE!=='test')throw new Error('Test branch writes are unavailable in production.');const parents=writeMode==='test-branch'?sortedRevisionIds(explicitParents):heads\n  if(revisions.some(existing=>existing.revision_id===revisionId))return\n  const recordData=revisionData(value),createdAt=new Date().toISOString(),revision:Revision=writeMode==='merge'?createMergeRevision(revisionId,parents.map(parent=>{const found=graph.revisions.get(parent);if(!found)throw new Error('Merge parent is missing.');return found}),recordData,createdAt):{record_type:profile.recordType,record_schema:profile.recordSchema,record_id:recordId,revision_id:revisionId,parent_revision_ids:parents,record_status:value.status==='deleted'?'deleted':'active',record_data:recordData,migration_origin:null,protocol_created_at:createdAt}\n  validateRevision(revision);validateRevisionGraph([...revisions,revision])\n  if(revision.record_status==='deleted'){if(revision.record_data!==null)throw new Error('Deleted records must contain null data.')}else validateDomainData(DOMAIN_SCHEMAS[revision.record_schema],revision.record_data)",
  'persist conflict logic')
source=replaceOnce(source,
  "function logicalAppId(revision:Revision,store:LocalStoreName):string{if(store===LOCAL_STORES.settings){if(revision.record_type==='activity_type_settings')return'activity-types';if(revision.record_type==='pain_type_settings')return'pain-types'}return revision.record_id}\nasync function readValues<T>",
  "export class UnresolvedRecordConflictError extends Error{constructor(readonly recordId:string,readonly headRevisionIds:readonly string[]){super(`Record ${recordId} has ${headRevisionIds.length} unresolved heads.`);this.name='UnresolvedRecordConflictError'}}\nfunction logicalAppId(revision:Revision,store:LocalStoreName):string{if(store===LOCAL_STORES.settings){if(revision.record_type==='activity_type_settings')return'activity-types';if(revision.record_type==='pain_type_settings')return'pain-types'}return revision.record_id}\nasync function readValues<T>",
  'conflict error insertion')
source=replaceOnce(source,
  "const graph=validateRevisionGraph(revisions),allowed=store===LOCAL_STORES.settings?new Set(['pain_type_settings','activity_type_settings']):new Set([STORE_PROFILE[store].recordType]),output:T[]=[];for(const ids of graph.headsByRecord.values())",
  "const graph=validateRevisionGraph(revisions),allowed=store===LOCAL_STORES.settings?new Set(['pain_type_settings','activity_type_settings']):new Set([STORE_PROFILE[store].recordType]);for(const [recordId,ids] of graph.headsByRecord){const first=graph.revisions.get(sortedRevisionIds(ids)[0]!);if(first&&allowed.has(first.record_type)&&ids.size>1)throw new UnresolvedRecordConflictError(recordId,sortedRevisionIds(ids))}const output:T[]=[];for(const ids of graph.headsByRecord.values())",
  'read conflict guard')
source=replaceOnce(source,
  "export async function getRecord<T>(storeName:LocalStoreName,id:string):Promise<T|undefined>{return(await getAllRecords<T&{id:string}>(storeName)).find(value=>value.id===id)}\nexport function putRecord",
  "export async function getRecord<T>(storeName:LocalStoreName,id:string):Promise<T|undefined>{return(await getAllRecords<T&{id:string}>(storeName)).find(value=>value.id===id)}\nexport interface RecordConflictHead<T=unknown>{revisionId:string;status:'active'|'deleted';data:T|null}\nexport interface RecordConflict<T=unknown>{recordId:string;heads:readonly RecordConflictHead<T>[]}\nasync function lookupRecordId(context:EpochContext,revisions:readonly Revision[],store:LocalStoreName,id:string):Promise<string>{const profile=profileFor(store,id);if(store===LOCAL_STORES.settings)return recordIdentity(context,store,id);return revisions.some(revision=>revision.record_id===id&&revision.record_type===profile.recordType&&revision.record_schema===profile.recordSchema)?id:recordIdentity(context,store,id)}\nexport async function getRecordConflict<T=unknown>(storeName:LocalStoreName,id:string):Promise<RecordConflict<T>|null>{await ready();const db=await openDatabase(),loaded=await loadEpoch(db),revisions=await verifiedEnvelopeRevisions(db),recordId=await lookupRecordId(loaded.context,revisions,storeName,id),graph=validateRevisionGraph(revisions),headIds=sortedRevisionIds(graph.headsByRecord.get(recordId)??[]);if(headIds.length<2)return null;const heads=headIds.map(revisionId=>{const revision=graph.revisions.get(revisionId);if(!revision||revision.record_status==='control')throw new Error('Conflict head is missing or invalid.');return{revisionId,status:revision.record_status,data:revision.record_data as T|null}});return{recordId,heads}}\nexport async function mergeRecord<T extends{id:string}>(storeName:LocalStoreName,value:T):Promise<void>{await ready();const db=await openDatabase(),loaded=await loadEpoch(db);await withDiaryLock(loaded.context.diaryId,async()=>persistRevision(db,storeName,value as unknown as Record<string,unknown>,undefined,'merge'))}\nexport function putRecord",
  'conflict api exports')
source=replaceOnce(source,
  "export const __localDatabaseTesting={openDatabase,loadEpoch,STORES,resetForTesting:resetDatabaseForTesting,setLegacyMigrationHook:setLegacyMigrationHookForTesting}",
  "async function appendTestBranch(store:LocalStoreName,value:{id:string}&Record<string,unknown>,parentRevisionId:string):Promise<void>{if(import.meta.env.MODE!=='test')throw new Error('Test branch writes are unavailable in production.');await ready();const db=await openDatabase(),loaded=await loadEpoch(db);await withDiaryLock(loaded.context.diaryId,async()=>persistRevision(db,store,value,undefined,'test-branch',[parentRevisionId]))}\nasync function revisionsForTesting():Promise<Revision[]>{if(import.meta.env.MODE!=='test')throw new Error('Revision inspection is test-only.');await ready();return verifiedEnvelopeRevisions(await openDatabase())}\nexport const __localDatabaseTesting={openDatabase,loadEpoch,STORES,resetForTesting:resetDatabaseForTesting,setLegacyMigrationHook:setLegacyMigrationHookForTesting,appendTestBranch,revisions:revisionsForTesting}",
  'test conflict seam')
fs.writeFileSync(path,source)

const test=`import 'fake-indexeddb/auto'\nimport { beforeEach,describe,expect,it } from 'vitest'\nimport { fromBase64Url } from '../security/crypto/bytes'\nimport { __localDatabaseTesting,getAllRecords,getRecord,getRecordConflict,LOCAL_STORES,mergeRecord,putRecord,UnresolvedRecordConflictError } from '../data/localDatabase'\n\nconst pain=(id:string,note:string)=>({id,startedAt:'2026-09-16T10:00:00.000Z',endedAt:'',locations:[],intensity:4,qualities:[],cause:'',occursWhen:'',note,createdAt:'2026-09-16T10:00:00.000Z',updatedAt:'2026-09-16T10:00:00.000Z'})\nfunction byteSort(ids:string[]):string[]{return [...ids].sort((left,right)=>{const a=fromBase64Url(left),b=fromBase64Url(right);for(let i=0;i<Math.min(a.length,b.length);i++){if(a[i]!==b[i])return a[i]!-b[i]!}return a.length-b.length})}\nasync function reset(){await __localDatabaseTesting.resetForTesting();await new Promise<void>((resolve,reject)=>{const request=indexedDB.deleteDatabase('eds-diary');request.onsuccess=()=>resolve();request.onerror=()=>reject(request.error);request.onblocked=()=>reject(new Error('blocked'))})}\n\ndescribe('explicit record conflict merge',()=>{beforeEach(reset)\nit('preserves sibling heads, blocks implicit reads/writes, and resolves only through an all-head merge',async()=>{await putRecord(LOCAL_STORES.painEntries,pain('local-seed','R5'));const initial=(await getAllRecords<ReturnType<typeof pain>>(LOCAL_STORES.painEntries))[0]!,recordId=initial.id;let revisions=(await __localDatabaseTesting.revisions()).filter(revision=>revision.record_id===recordId),base=revisions[0]!;await putRecord(LOCAL_STORES.painEntries,{...initial,note:'R6A',updatedAt:'2026-09-16T11:00:00.000Z'});revisions=(await __localDatabaseTesting.revisions()).filter(revision=>revision.record_id===recordId);const headA=revisions.find(revision=>revision.parent_revision_ids.includes(base.revision_id))!;await __localDatabaseTesting.appendTestBranch(LOCAL_STORES.painEntries,{...initial,note:'R6B',updatedAt:'2026-09-16T12:00:00.000Z'},base.revision_id);const conflict=await getRecordConflict<Record<string,unknown>>(LOCAL_STORES.painEntries,recordId);expect(conflict?.heads).toHaveLength(2);expect(new Set(conflict?.heads.map(head=>(head.data as {note?:string}|null)?.note))).toEqual(new Set(['R6A','R6B']));await expect(getRecord(LOCAL_STORES.painEntries,recordId)).rejects.toBeInstanceOf(UnresolvedRecordConflictError);await expect(getAllRecords(LOCAL_STORES.painEntries)).rejects.toBeInstanceOf(UnresolvedRecordConflictError);await expect(putRecord(LOCAL_STORES.painEntries,{...initial,note:'implicit winner',updatedAt:'2026-09-16T13:00:00.000Z'})).rejects.toBeInstanceOf(UnresolvedRecordConflictError);const headIds=conflict!.heads.map(head=>head.revisionId);await mergeRecord(LOCAL_STORES.painEntries,{...initial,note:'R7 merged',updatedAt:'2026-09-16T14:00:00.000Z'});revisions=(await __localDatabaseTesting.revisions()).filter(revision=>revision.record_id===recordId);const merged=revisions.at(-1)!;expect(merged.parent_revision_ids).toEqual(byteSort(headIds));expect(revisions.map(revision=>revision.revision_id)).toEqual(expect.arrayContaining([base.revision_id,headA.revision_id,...headIds,merged.revision_id]));expect(await getRecord<{id:string;note:string}>(LOCAL_STORES.painEntries,recordId)).toMatchObject({id:recordId,note:'R7 merged'});expect(await getRecordConflict(LOCAL_STORES.painEntries,recordId)).toBeNull()})})\n`
fs.writeFileSync('src/test/conflictMerge.test.ts',test)
